export default {
	accordion: {
		colorScheme: {
			light: {
				header: {
					background: "var(--card-bg-glass)",
					activeBackground: "var(--card-bg-glass)",
					hoverBackground: "var(--card-bg-glass)",
					activeHoverBackground: "var(--card-bg-glass)",
				}
			},
			dark: {
				header: {
					background: "var(--card-bg-glass)",
					activeBackground: "var(--card-bg-glass)",
					hoverBackground: "var(--card-bg-glass)",
					activeHoverBackground: "var(--card-bg-glass)",
				}
			},
		}
	}
};
